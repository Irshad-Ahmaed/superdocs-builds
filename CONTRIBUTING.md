# Contributing

Thanks for building on SuperDocs. This repo is intentionally simple: one builder, one folder, one pull request.

## Where your work goes

1. Fork this repository.
2. Create your folder inside the section that fits your project:
   - `use-cases/<your-github-username>/<project-name>/` for apps and use cases
   - `extensions/<your-github-username>/<project-name>/` for extensions, integrations and developer tools
3. Build inside your folder only. Do not modify anything outside it.
4. Open one pull request per project.

More than one project? Add more project folders under your username and open a separate pull request for each. Several people building similar things is fine here; every take on an idea is welcome.

## Your pull request description must include

- **Your name** (the one you want publicly credited)
- **A contact email** (use the same email you use with SuperDocs, so we can connect your work to you)
- One or two sentences on what the project does

We read every pull request. Work that cannot be traced to its builder cannot be credited, so the name and email matter.

## Every project needs its own README

Inside your project folder, a `README.md` with:

- What it does, in plain language
- How to run it (setup, environment variables as placeholders, commands)
- What SuperDocs features it uses (chat editing, API, MCP, export, and so on)
- A link to a demo, video or live deployment if you have one

## Ground rules

- **Never commit secrets.** No API keys, no tokens, no passwords, not even in history. Use placeholders like `SUPERDOCS_API_KEY=your-key-here`.
- Using AI tools to build is welcome and encouraged. Build the way you actually build.
- Keep third-party code properly attributed and license-compatible with MIT.
- Be respectful in reviews and discussions.

## License

By contributing you agree your contribution is published under the repository's MIT license, credited to you.
